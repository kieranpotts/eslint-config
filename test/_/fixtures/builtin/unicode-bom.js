﻿/*
https://eslint.org/docs/latest/rules/unicode-bom

All JavaScript/TypeScript files SHOULD be UTF-8 encoded, and since UTF-8 does
not require a BOM (Byte Order Mark), we MAY exclude this character from the
start of our source files.
*/
